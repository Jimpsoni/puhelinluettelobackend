// These are mock tests for CICD pipeline
test('Mock tests exists', () => {
    expect(1).toBe(1)
})

test('1 + 1 does equal 2', () => {
    expect(1 + 1).toBe(2)
})

test('Hello world is the common phrase for developers', () => {
    expect("hello world").toBe("hello world")
})
